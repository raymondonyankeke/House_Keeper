<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.java.housekeeper">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".admin.CreateCategoryActivity"></activity>
        <activity android:name=".ui.maid_detailed.ShowServicesActivity" />
        <activity android:name=".ui.profile.ChangeEmailActivity" />
        <activity android:name=".ui.profile.ChangePasswordActivity" />
        <activity android:name=".ui.maid_rating.MaidRatingActivity" />
        <activity android:name=".admin.EditMaidActivity" />
        <activity android:name=".admin.AddServicesActivity" />
        <activity android:name=".ui.maid_detailed.MaidDetailedActivity" />
        <activity android:name=".ui.profile.CreateUserMaidActivity" />
        <activity android:name=".admin.AddMaidActivity" />
        <activity
            android:name=".admin.AdminActivity"
            android:label="Admin Panel"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignUpActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>